import { EvilDirective } from './evil.directive';

describe('EvilDirective', () => {
  it('should create an instance', () => {
    const directive = new EvilDirective();
    expect(directive).toBeTruthy();
  });
});
